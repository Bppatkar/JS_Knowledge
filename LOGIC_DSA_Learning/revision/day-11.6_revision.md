# Day 11.6 Revision — Prefix Hash

For longest subarray:
```text
prefix → earliest index
```

Why?
Earlier start creates a longer distance to the current index.

Never overwrite the first prefix index when longest length is required.
