# Day 11.8 Revision — Phase 2 Completion

## 5-Minute Recall
- Map = key → value
- Set = presence/uniqueness
- Prefix formula: `previous = currentPrefix - target`
- Longest range: store earliest index
- Window + Hash: maintain active state
- 3Sum: sort + two pointers
- Custom Hash: hash → bucket → collision handling

## Blind Recall
1. Why `map.set(0, -1)`?
2. Why earliest index for longest?
3. What does `currentPrefix - target` represent?
4. When should Map store frequency?
5. Why can negative numbers affect sliding-window assumptions?
6. Why is 3Sum O(n²)?
7. What is a collision?
8. Why don't we search all custom-hash buckets?

## CCRS
Short recall → one blind problem → attempt → feedback.
